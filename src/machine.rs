/*
use crate::data::*;

enum Context {
    Fst,
    Snd,
    LeftTimes,
    RightTimes,
    LeftPlus,
    RightPlus,
    Trace,
}

struct ContextStack {
    stack: Vec<Context>,
}

struct Machine {
    direction: bool,
    circuit: Code,
    token: Data,
    context: ContextStack,
}

impl Machine {
    pub fn new(img: Data) -> Result<Self, ()>
    {
    }

    pub fn exec() -> Result<Data, ()>
    {
    }
}
*/
